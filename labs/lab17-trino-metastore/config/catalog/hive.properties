# Trino "hive" catalog — read tables registered in the SAME Hive metastore, straight from HDFS.
connector.name=hive
hive.metastore.uri=thrift://hive-metastore:9083
# Allow creating/writing tables too (not just reading).
hive.non-managed-table-writes-enabled=true
